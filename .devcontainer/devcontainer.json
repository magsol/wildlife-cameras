{
    // Custom VS Code dev environment for the Raspberry Pi.
    "name": "Picamera2 Development",
    "build": {
        // Path is relative to this file.
        "dockerfile": "Dockerfile"
    },
    "runArgs": ["--platform=linux/amd64"]
}